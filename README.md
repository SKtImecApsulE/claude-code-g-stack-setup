# Personal Assistant

A personal assistant project. Scope and design are not yet defined.

## Status

- Repo bootstrapped with [gstack](https://github.com/garrytan/gstack) for AI-assisted development (see `CLAUDE.md`).
- Next step: define what the assistant should do — run `/office-hours` or `/spec` in Claude Code to turn the idea into a concrete plan.
